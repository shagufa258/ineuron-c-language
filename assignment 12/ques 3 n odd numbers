//3. Write a recursive function to print first N odd natural numbers

#include<stdio.h>
int num(int);
int main()
{
  // printf("%d",num(5)) ;
  num(5);
}

int num(int n)
{
    if (n==0)
        return 1;
    
    printf("%d, ",2*n-1);  
    num(n-1);
    
}
