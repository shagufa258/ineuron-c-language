2. Write a recursive function to print first N natural numbers in reverse order

#include<stdio.h>
int num(int);
int main()
{
  // printf("%d",num(5)) ;
  num(5);
}

int num(int n)
{
    if (n==0)
        return 1;
    
    printf("%d, ",n);  
    num(n-1);
    
}
