//Write a recursive function to print squares of first N natural numbers

#include<stdio.h>
int num(int);
int main()
{
  // printf("%d",num(5)) ;
  num(5);
}

int num(int n)
{
    if (n==0)
        return 1;
    
    
    num(n-1);
     printf("%d, ",n*n); 
    
}
