#include <stdio.h>

int main()
{
   int a=-1,b=1,c, num=10, bin=0, place=1;
   while(num)
   {  
       c=a+b;
       printf("%d  ", c);
       a=b;
       b=c;
       num--;
     
   }


    return 0;
}
