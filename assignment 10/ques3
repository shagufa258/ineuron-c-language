/************3. Write a function to check whether a given number is even or odd. Return 1 if the
number is even, otherwise return 0. (TSRS)*******************/

#include <stdio.h>
int check(int num);

int main()


{   printf("returns = %d ",check(10));

    return 0;
}

int check(int num)
{
    
   if(num%2==0)
        return 1;
    else
        return 0;
        
}
