2. Write a program to calculate the average of numbers stored in an array of size n.
Take array values from the user.

#include <stdio.h>

int main()
{int n,a[100],sum=0;
     printf("enter size of array");
    scanf("%d",&n);
     
     
     for(int i=0; i<n;i++)
     {
          scanf("%d",&a[i]);
          printf("index=%d element= %d\n", i, a[i]);
     }
     
     
     for(int i=0; i<n;i++)
     {
         
          printf("%d ",a[i]);
          sum+=a[i];
     }
      printf("\navg=%d ",sum/n);
     
     return 0;
}
